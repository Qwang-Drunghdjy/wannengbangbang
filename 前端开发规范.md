# 万能帮帮 - 前端开发规范文档

> **文档用途**：为 AI Agent 提供完整的页面结构、交互逻辑、数据模型和组件规范，用于 Web 应用前端开发。  
> **技术栈建议**：Vue 3 + TypeScript + Tailwind CSS（或等效 UI 库）。  
> **版本**：v1.0  
> **最后更新**：2026-08-12

---

## 1. 全局设计规范

### 1.1 配色方案
- **主色**：`#2563EB`（蓝色，用于按钮、顶部栏背景、高亮元素）
- **背景色**：`#F8FAFC`（浅灰，用于页面背景）
- **卡片背景**：`#FFFFFF`（白色）
- **文字主色**：`#1E293B`（深灰）
- **文字辅助色**：`#64748B`（中灰）
- **成功/匹配高亮**：`#10B981`（绿色，用于匹配度标签）
- **未读标记**：`#EF4444`（红色，小红点）

### 1.2 字体与圆角
- **字体**：系统默认字体栈（`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`）
- **圆角**：按钮和卡片统一 `8px`，头像圆形 `50%`

### 1.3 导航与返回
- 所有二级页面（发布页、匹配结果页、消息通知页、个人中心页）顶部均包含 **返回按钮**（←），点击返回上一页。
- 底部导航栏仅出现在 **首页**，其他页面不显示底部导航。

---

## 2. 页面路由与层级

| 路由路径             | 页面名称       | 是否显示底部导航 |
|----------------------|----------------|------------------|
| `/`                  | 首页           | 是               |
| `/publish`           | 发布页         | 否               |
| `/match-result`      | 匹配结果页     | 否               |
| `/messages`          | 消息通知页     | 否               |
| `/profile`           | 个人中心页     | 否               |
| （弹窗）             | 帮帮柜介绍弹窗 | 不涉及路由       |

---

## 3. 页面详细规格

### 3.1 首页 (`/`)

#### 3.1.1 顶部区域
- **左上角**：文本 `"万能帮帮"`，字体加粗，字号 `20px`。
- **右上角**：定位图标（📍）+ 文本 `"南京审计大学"`，点击可触发位置选择（暂不实现）。
- **顶部下方**：副标题 `"让失物有处寻 · 求助有回应"`，居中，字号 `14px`，颜色 `#64748B`。

#### 3.1.2 四个功能入口（2行×2列网格）
- 每个入口包含一个 **图标（Emoji）** 和 **文字标签**。
- 点击行为：
  - **发布失物**（📷）→ 跳转 `/publish?type=lost`
  - **发布拾物**（📦）→ 跳转 `/publish?type=found`
  - **帮帮柜**（🤝）→ 触发 **帮帮柜介绍弹窗**（见第 7 节）
  - **我的**（👤）→ 跳转 `/profile`

#### 3.1.3 失物列表区域
- 标题：`"最近失物"`，字号 `18px`，加粗。
- 列表项（3条示例数据，实际由 API 提供）：
  - 每条显示：**图标（Emoji）+ 物品名称** | **地点** | **相对时间**
  - 示例：
    - 🎒 黑色双肩包 | 图书馆 | 2小时前
    - 📱 白色iPhone | 食堂 | 5小时前
    - 🔑 钥匙串 | 操场 | 1天前
- 列表可滚动，支持下拉刷新（暂不实现）。

#### 3.1.4 底部导航栏（4项）
- 固定底部，高度 `60px`，背景白色，上边框 `1px solid #E2E8F0`。
- 每一项：图标 + 文字，点击切换页面。
- 高亮项：`首页` 默认高亮（图标和文字颜色为主色 `#2563EB`）。
- 导航项：
  - 🏠 首页 → `/`（当前页高亮）
  - 🔔 消息 → `/messages`
  - 📋 发布 → `/publish`（无预设类型，进入后用户选择）
  - 👤 我的 → `/profile`

---

### 3.2 发布页 (`/publish`)

#### 3.2.1 URL 参数
- `?type=lost` 或 `?type=found`，用于确定发布类型，影响提交时的 `category` 字段。

#### 3.2.2 顶部导航栏
- 左侧：返回按钮（←），点击 `window.history.back()`
- 中间：标题 `"发布信息"`
- 右侧：空

#### 3.2.3 上传图片区域
- 虚线边框区域，高度 `120px`，居中显示 `"📷 点击上传图片"`。
- 点击后触发文件选择器（仅图片），演示阶段弹出 `alert("已上传")` 并显示缩略图（占位）。
- 支持多图上传（最多 4 张），缩略图横向排列。

#### 3.2.4 表单字段
| 字段名         | 组件类型     | 是否必填 | 占位文字 / 选项                                     |
|----------------|--------------|----------|-----------------------------------------------------|
| 物品名称       | 文本输入框   | 是       | "请输入物品名称"                                    |
| 详细描述       | 多行文本框   | 否       | "请描述物品的颜色、品牌、特征..."，行数 4           |
| 丢失/拾到地点  | 下拉选择框   | 是       | 选项：图书馆、食堂、教学楼、操场、宿舍、商场、社区、其他 |
| 联系方式       | 文本输入框   | 是       | "手机号/微信号"                                     |

#### 3.2.5 提交按钮
- 蓝色背景（`#2563EB`），白色文字，圆角，宽度 100%，高度 `48px`。
- 点击后：
  - 前端校验必填字段。
  - 模拟提交，跳转至 `/match-result`（携带模拟数据）。

---

### 3.3 匹配结果页 (`/match-result`)

#### 3.3.1 顶部区域
- 标题：`"🎉 匹配成功！"`，字号 `24px`，居中。
- 副标题：`"已为您找到以下匹配物品"`，字号 `14px`，颜色 `#64748B`，居中。

#### 3.3.2 匹配结果卡片
- **主卡片**（大号，高亮边框）：
  - 左侧：物品图片（占位图，可用灰色方块 + 📦 emoji）
  - 右侧信息：
    - 物品名称：`"黑色双肩包"`
    - 匹配度：⭐ 95%（绿色文字）
    - 地点：`图书馆 2楼`
    - 时间：`2小时前`
  - 底部按钮：`"🔗 联系TA"`，蓝色背景，点击弹出提示（模拟联系）。
- **下方小卡片×2**（横向排列或纵向）：
  - 卡片1：`白色水杯 | 匹配度78% | 食堂`
  - 卡片2：`蓝色笔袋 | 匹配度62% | 教学楼`
  - 样式：白色背景，圆角，字体较小，点击可切换主卡片（交互暂不实现）。

#### 3.3.3 底部按钮
- `"返回首页"`，灰色边框按钮，点击跳转 `/`。

---

### 3.4 消息通知页 (`/messages`)

#### 3.4.1 顶部导航栏
- 左侧：返回按钮（←）
- 中间：`"我的消息"`
- 右侧：`"全部已读"` 文字按钮，点击将所有未读标记为已读（前端模拟）。

#### 3.4.2 消息列表（按时间倒序）
- 每条消息包含：
  - 头像（👤 占位）
  - 标题（加粗）
  - 内容摘要（单行省略）
  - 相对时间
  - 未读状态：显示红色小红点（圆点 `●`）在标题左侧。
- 示例数据（硬编码，待 API 替换）：
  1. **未读**：标题 `【匹配成功】您的背包已找到！`，摘要 `"拾物者已存入帮帮柜，请凭取件码..."`，时间 `10分钟前`
  2. **已读**：标题 `【互助请求】求教高数题`，摘要 `"同学你好，看到你发布的信息..."`，时间 `2小时前`
  3. **已读**：标题 `【系统通知】您的物品已存放超时`，摘要 `"请于24小时内取回..."`，时间 `1天前`

---

### 3.5 个人中心页 (`/profile`)

#### 3.5.1 顶部区域（蓝色背景 `#2563EB`）
- 头像：圆形，直径 `64px`，显示 `"👤"` 占位。
- 昵称：`"拾光者用户"`，字号 `18px`，白色。
- 信用评分：⭐ `4.8分`，字号 `14px`，白色。

#### 3.5.2 功能列表（白色卡片）
- 每个列表项为一行，左侧图标 + 文字，右侧显示附加信息 + 箭头（→）。
- 列表项：
  - 📋 我的发布 → 右侧 `3条 →`（点击跳转至我的发布列表页，暂不实现）
  - 🔗 我的匹配 → 右侧 `5次 →`（点击跳转至匹配记录页，暂不实现）
  - ⭐ 信用评分 → 右侧 `4.8分 →`（点击跳转至信用详情，暂不实现）
  - ⚙️ 设置 → 右侧 `→`（点击跳转至设置页，暂不实现）

#### 3.5.3 底部
- `"退出登录"` 文字按钮，灰色，居中，点击弹出确认框，确认后跳转至登录页（暂不实现）。

---

## 4. 帮帮柜介绍弹窗（非独立页面）

### 4.1 触发方式
- 首页点击 `🤝 帮帮柜` 入口时弹出。

### 4.2 弹窗内容
- **标题**：`"📦 万能帮帮柜"`
- **功能说明**（文本）：
  - 24小时自助存取
  - 扫码即可开柜
  - 全程录像追溯
- **使用流程**（3步，带数字标号）：
  1. 小程序发布/匹配成功 → 获得取件码
  2. 到帮帮柜扫码/输码 → 柜门弹开
  3. 存入或取出物品 → 关闭柜门
- **部署位置**：`"图书馆、食堂、宿舍楼下、商场、社区"`
- **底部按钮**：`"我知道了"`，点击关闭弹窗。

---

## 5. 数据模型（TypeScript 接口）

```typescript
// 物品发布
interface PublishItem {
  id: string;
  category: 'lost' | 'found';      // 失物 / 拾物
  name: string;                    // 物品名称
  description?: string;            // 详细描述
  location: string;                // 地点（枚举值）
  contact: string;                // 联系方式
  images?: string[];              // 图片 URL 数组
  createdAt: string;              // ISO 时间
}

// 匹配结果
interface MatchResult {
  item: PublishItem;
  matchScore: number;             // 0-100
  matchedItem?: PublishItem;      // 匹配到的另一条记录
}

// 消息
interface Message {
  id: string;
  title: string;
  summary: string;
  isRead: boolean;
  createdAt: string;              // ISO 时间
  type: 'match' | 'help' | 'system';
}

// 用户信息
interface UserProfile {
  nickname: string;
  avatar?: string;                // URL
  creditScore: number;            // 0-5
  publishCount: number;
  matchCount: number;
}
```

---

## 6. 交互与状态管理要点

### 6.1 全局状态（建议使用 Context / Store）
- 当前用户信息（`UserProfile`）
- 未读消息数量（用于底部导航小红点角标）
- 发布类型（`lost` / `found`）在跳转发布页时通过 URL 参数传递

### 6.2 路由守卫
- 无需登录即可访问所有页面（演示阶段）。

### 6.3 数据持久化
- 演示阶段所有数据使用 **Mock 数据**，不涉及后端 API。
- 后续接入 API 时，替换 Mock 数据为 fetch 请求。

---

## 7. 组件拆分建议（按页面）

### 7.1 公共组件
- `BottomNav`（底部导航，仅首页使用）
- `TopBar`（顶部导航，含返回按钮、标题、右侧操作）
- `UploadArea`（图片上传组件）
- `MatchCard`（匹配结果卡片，分大小两种尺寸）
- `MessageItem`（消息列表项）

### 7.2 页面级组件
- `HomePage`
- `PublishPage`
- `MatchResultPage`
- `MessagesPage`
- `ProfilePage`
- `CabinetDialog`（帮帮柜弹窗）

---

## 8. 开发注意事项

1. **图标**：Emoji 用于演示，正式开发可替换为 SVG 图标库（如 Heroicons、Font Awesome）。
2. **响应式**：页面适配移动端（375px ~ 428px 宽度），桌面端居中且最大宽度 `480px`。
3. **表单验证**：提交前校验必填字段，给出提示。
4. **匹配度显示**：用星级（⭐）和百分比数值，颜色根据数值变化（>80% 绿色，60-80% 橙色，<60% 灰色）。
5. **时间显示**：使用 `dayjs` 或 `date-fns` 转换为相对时间（如 “2小时前”）。
6. **弹窗**：帮帮柜弹窗为模态框，点击遮罩或“我知道了”关闭。

---

## 9. 示例 API 接口（Mock 用）

```javascript
// 获取最近失物列表
GET /api/items/recent?limit=3
Response: { items: PublishItem[] }

// 提交发布
POST /api/publish
Body: PublishItem
Response: { success: true, matchResults: MatchResult[] }

// 获取消息列表
GET /api/messages
Response: { messages: Message[] }

// 标记全部已读
POST /api/messages/read-all
Response: { success: true }
```

---

## 10. 测试数据（用于演示）

- 失物列表（首页）：见 3.1.3。
- 匹配结果主卡片：黑色双肩包（95%），匹配地点“图书馆 2楼”，时间“2小时前”。
- 小卡片：白色水杯（78%，食堂），蓝色笔袋（62%，教学楼）。
- 消息列表：见 3.4.2。
- 个人中心：昵称“拾光者用户”，信用 4.8 分，发布 3 条，匹配 5 次。

---

**文档结束**  