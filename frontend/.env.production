# 生产公共访问域名（前端部署地址；供「复制链接」拼绝对链接用）
# 留空则回退到 window.location.origin（即当前部署域名，含临时域名）。
# 建议注册正式域名后在此填写，如 VITE_PUBLIC_BASE_URL=https://www.example.com
VITE_PUBLIC_BASE_URL=

# 生产后端 API 基础路径（如与前端同域经网关转发可留空；否则填后端公开地址）
VITE_API_BASE=/api/v1